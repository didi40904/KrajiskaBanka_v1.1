# KrajiskaBanka_v1.1
This is an ATM simulation that uses a 6 digit integer instead of a credit car. Works and depends on a database. Everything is bundled with setup files and will work without SQL server or VS. It is not failsafe but it is functional. Variable names are written in Serbian (sorry).
